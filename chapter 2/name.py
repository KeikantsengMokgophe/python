name = "ada lovelace"
print(name.upper())
print(name.title())
print(name.lower())

# In this program, I create a variable called name and store the text "ada lovelace" in it.

# name = "ada lovelace"

# A variable is used to store data that can be accessed and manipulated later in the program.

# Next, I use three different string methods to display the text in different formats.

# print(name.upper())

# The upper() method converts every letter in the string to uppercase. The output will be:

# ADA LOVELACE

# Then I use:

# print(name.title())

# The title() method capitalizes the first letter of each word and converts the remaining letters to lowercase. The output will be:

# Ada Lovelace

# Finally:

# print(name.lower())

# The lower() method converts all letters in the string to lowercase. Since the original text is already lowercase, the output remains:

# ada lovelace
# Complete Output
# ADA LOVELACE
# Ada Lovelace
# ada lovelace